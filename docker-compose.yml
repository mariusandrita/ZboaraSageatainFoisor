version: '3.9'

services:
  dartsleague:
    build: .
    ports:
      - "80:80"
    volumes:
      - db_data:/data
    environment:
      - NODE_ENV=production
      - PORT=80
      - DB_PATH=/data/dartsleague.db
      - LOG_LEVEL=info
    restart: unless-stopped

volumes:
  db_data:
